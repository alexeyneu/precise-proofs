precise-proofs reference

...

10. oneof field set
in golang it's represented by an interface variable
It could been set to instance of the one of types each named after a field. These are structures and each contains one field same as one in field set (name may vary slightly) .
 To use its fields for proofs field that has been set is used. If oneof fieldset is unset it's ignored so can't be used.
